version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto8
  build:
    commands:
      - echo Entered the build phase...
      - mvn test
  post_build:
    commands:
      - echo Build completed successfully
      - mvn package
artifacts:
  files:
    - target/order-service.jar
  discard-paths: yes
